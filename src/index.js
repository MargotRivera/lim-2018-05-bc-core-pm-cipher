const cifrar = document.getElementById('cipher');
cifrar.addEventListener('click',window.cipher.encode);
const entrar = document.getElementById('enter');
entrar.addEventListener('click',window.cipher.entrar); 